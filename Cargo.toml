[package]
name = "linglun"
version = "0.1.0"
edition = "2024"
license = "Unlicense"

[dependencies]
pest = "2.8.6"
pest_derive = "2.8.6"

# PDF 生成
pdf-writer = "0.14.0"

# 字体支持
owned_ttf_parser = "0.25.0"

# daemon 模式依赖
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
